spring:
  jpa:
    properties:
      'hibernate.criteria.literal_handling_mode': bind

logging:
  level:
    com.github.mhewedy.expressions: debug
    org.hibernate.type.descriptor.sql.BasicBinder: trace